---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: userauthenticationbinds.kargus.io
spec:
  group: kargus.io
  names:
    kind: UserAuthenticationBind
    listKind: UserAuthenticationBindList
    plural: userauthenticationbinds
    singular: userauthenticationbind
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.user
      name: User
      type: string
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .status.sv.status
      name: Phase
      type: string
    - jsonPath: .status.sv.expiresAt
      name: Expires
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: UserAuthenticationBind is the Schema for the userauthenticationbinds
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of UserAuthenticationBind
            properties:
              domain:
                description: domain is the domain the user authenticates against.
                minLength: 1
                type: string
              memberships:
                description: memberships are the group memberships granted to the
                  user by this bind.
                items:
                  description: Membership represents a single group the user is bound
                    to.
                  properties:
                    domain:
                      description: domain is the domain that owns the group.
                      minLength: 1
                      type: string
                    gid:
                      description: gid is the stable group identifier (e.g. "group/g12312312").
                      minLength: 1
                      type: string
                    name:
                      description: name is the human-readable group name (e.g. "engineering").
                      minLength: 1
                      type: string
                  required:
                  - domain
                  - gid
                  - name
                  type: object
                type: array
                x-kubernetes-list-type: atomic
              ttl:
                description: |-
                  ttl is the lifetime of the authentication bind before it expires
                  (e.g. "12h", "30m"). After the TTL elapses the bind is unbound.
                pattern: ^[0-9]+(ns|us|µs|ms|s|m|h)$
                type: string
              user:
                description: user is the user being bound (e.g. "lucas").
                minLength: 1
                type: string
            required:
            - domain
            - ttl
            - user
            type: object
          status:
            description: status defines the observed state of UserAuthenticationBind
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the UserAuthenticationBind resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              sv:
                description: sv holds the observed state of the external bind.
                properties:
                  expiresAt:
                    description: expiresAt is the time at which this bind expires
                      (creation + ttl).
                    format: date-time
                    type: string
                  ref:
                    description: ref is the external reference returned by the binding
                      service.
                    type: string
                  status:
                    description: status is the current phase of the service bind.
                    enum:
                    - pending
                    - binding
                    - binded
                    - unbound
                    - failed
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
